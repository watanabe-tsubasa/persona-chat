// File: next.config.ts
// Role: Next.js configuration (App Router, Turbopack enabled via scripts)
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
